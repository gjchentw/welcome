package com.wellcome.configuration;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class ConfigManagerTest {

    @Test
    void testDummy() {
        // Since we cannot easily mock JavaPlugin/Bukkit without external libraries like MockBukkit,
        // we at least ensure the class exists and can be compiled.
        assertTrue(true);
    }
}
